name: flappybirdgame
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.26.1  # Dodao Flame zavisnost
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Ažurirana verzija flutter_lints

flutter:
  uses-material-design: true
  assets:
    - assets/images/
